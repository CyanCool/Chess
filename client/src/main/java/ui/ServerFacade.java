package ui;

public class ServerFacade
{
    public ServerFacade(){}

    public void register(String userInformation)
    {
        //Make HTTP request to the server to register
        //translate the user information into HTTP?

        //throws errors here, catches it in prelogin

        //If no errors, makes a register request and login request to register and login the user with the API
        //If successfully registered, log the client in with no issues
        //Make a post login object and call its run function
    }
}
